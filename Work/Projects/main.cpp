#include "func.h"

using namespace std;

int main() {

    MyArray test;

    test.create();

    cout << "Size of array is " << test.size() << endl;

    test.display();

    cout << "Input integer to add an element at the beginning of the array"


    return 0;
}
